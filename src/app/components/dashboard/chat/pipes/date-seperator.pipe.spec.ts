import { DateSeperatorPipe } from './date-seperator.pipe';

describe('DateSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
