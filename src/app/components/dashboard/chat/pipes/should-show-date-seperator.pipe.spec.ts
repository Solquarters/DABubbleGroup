import { ShouldShowDateSeperatorPipe } from './should-show-date-seperator.pipe';

describe('ShouldShowDateSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShouldShowDateSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
