import { GetMessageTimePipe } from './get-message-time.pipe';

describe('GetMessageTimePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMessageTimePipe();
    expect(pipe).toBeTruthy();
  });
});
